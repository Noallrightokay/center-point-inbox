apiVersion: v1
kind: Service
metadata:
  name: merger-service
  namespace: center-point-inbox
  labels:
    app.kubernetes.io/name: merger-service
    app.kubernetes.io/part-of: center-point-inbox
    app.kubernetes.io/component: core
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: merger-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
