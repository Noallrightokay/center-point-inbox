apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: center-point-inbox
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: center-point-inbox
    app.kubernetes.io/component: auth
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
